@model PracaInz.ViewModels.AdministrationViewModels.UserListItemViewModel
@inject PracaInz.Services.TicketServices TicketService

<div class="row">
    <div class="col-md-4">
        <form asp-action="EditUserConfirmed">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="FirstName" class="control-label"></label>
                <input asp-for="FirstName" class="form-control" />

            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label"></label>
                <input asp-for="LastName" class="form-control" />

            </div>
            <div class="form-group">
                <label asp-for="Department" class="control-label"></label>
                <input asp-for="Department" class="form-control" />
            </div>
            @foreach (var device in Model.Devices)
            {

                @if (Model.Devices.Count() > 0)
                {
                    <table class="table border border-info rounded">
                        <thead>
                            <tr>
                                <th>
                                    <p>Model</p>
                                </th>
                                <th>
                                    <p>Serial Number</p>
                                </th>
                                <th>
                                    <p>Additional options</p>
                                </th>
                            </tr>

                        </thead>
                        <tbody>
                            <tr>
                                <td>
                                    @device.Model
                                </td>
                                <td>
                                    <a asp-controller="Devices" asp-action="Edit" asp-route-id="@device.Id">@device.SerialNumber</a>
                                </td>
                                <td>
                                    <a class="btn btn-outline-danger" asp-controller="Devices" asp-action="Delete" asp-route-id="@device.Id">Delete</a>
                                </td>
                            </tr>

                        </tbody>
                    </table>

                }
                else
                {
                    <div class="row">
                        <div class="col-12">
                            <h5 style="text-align: center">No devices assigned</h5>
                        </div>
                    </div>

                }
            }
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
